import 'package:flutter/material.dart';

class AdminPanelStub extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Admin Panel (Stub)')),
      body: Center(child: Text('Use the provided React admin snippet for a full admin dashboard.')),
    );
  }
}
